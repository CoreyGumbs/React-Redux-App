export const dragonState = {
    isLoading: false,
    data: {},
    errors: {}
};

export const dragonReducer = ( state = dragonState, action) => {
    switch(action.type){
        default: 
            return state;
    }
};