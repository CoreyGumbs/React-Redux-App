export const rocketState = {
    isLoading: false,
    data: {},
    errors: ''
};

export const rocketReducer = ( state = rocketState, action) => {

    switch(action.type){
        default:
            return state;
    }

};